import { View } from "@tarojs/components";
import { useState } from "react";

interface NavProps {
  title: string;
  showBack: boolean = true;
  showNext: boolean = true;
}

function NavigationBar(props: ) {
  
  return (
    <View className='nav-wpt'>
      1111
    </View>
  )
}

export default NavigationBar;